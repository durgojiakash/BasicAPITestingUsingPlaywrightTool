

export function randomName(): String {
        let rname = "Test" + Math.floor(Math.random()*100);
        let randomName = rname;
        return randomName;
}

export function randomEmail(): String {
        let rname = "Test" + Math.floor(Math.random()*100);
        let randomEmail = rname + "@gmail.com"
        return randomEmail;
}
